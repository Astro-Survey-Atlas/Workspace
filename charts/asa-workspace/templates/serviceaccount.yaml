{{- if and .Values.dataWarehouse.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "asa-workspace.serviceAccountName" . }}
  labels:
    {{- include "asa-workspace.labels" . | nindent 4 }}
{{- end }}
