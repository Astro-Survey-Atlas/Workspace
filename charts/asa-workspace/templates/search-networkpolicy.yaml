{{- if eq .Values.search.mode "bundled" }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "asa-workspace.fullname" . }}-search
  labels:
    {{- include "asa-workspace.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ include "asa-workspace.elasticsearchName" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: master
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: {{ include "asa-workspace.name" . }}
              app.kubernetes.io/instance: {{ .Release.Name }}
      ports:
        - port: 9200
          protocol: TCP
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: {{ include "asa-workspace.elasticsearchName" . }}
              app.kubernetes.io/instance: {{ .Release.Name }}
              app.kubernetes.io/component: master
      ports:
        - port: 9200
          protocol: TCP
        - port: 9300
          protocol: TCP
{{- end }}
